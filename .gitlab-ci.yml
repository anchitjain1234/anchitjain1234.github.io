image: ruby:2.3

test:
  stage: test
  script:
  - gem install jekyll
  - gem install redcarpet
  - jekyll build -d test
  artifacts:
    paths:
    - test
  except:
  - master

pages:
  stage: deploy
  script:
  - gem install jekyll
  - gem install redcarpet
  - jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - master
